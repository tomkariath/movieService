pipeline {

	agent any
	
	stages{
        stage("Runing unit tests") {
			withMaven(maven : 'maven363'){
				sh 'mvn clean install'
			}
		}
            
        stage("Staging") {
            withMaven(maven : 'maven363'){
				sh 'mvn spring-boot:run'
			}
        }
    }
}